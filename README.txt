This is a simple to-do list page which keeps track of the day of the week and allows you to add items to your list as well as check them when you are done.

To run the page I use the command "nodemon app.js" and then access localhost:3000